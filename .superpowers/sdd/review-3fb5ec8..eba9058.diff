# Review package: 3fb5ec8..eba9058

## Commits
eba9058 feat(reader): capture last-read page thumbnail for Continue band

## Files changed
 src/lib/services/comicProcessor.ts | 8 +++++---
 src/routes/reader/+page.svelte     | 7 +++++++
 src/types/comic.ts                 | 1 +
 3 files changed, 13 insertions(+), 3 deletions(-)

## Diff
diff --git a/src/lib/services/comicProcessor.ts b/src/lib/services/comicProcessor.ts
index e825eb8..280244d 100644
--- a/src/lib/services/comicProcessor.ts
+++ b/src/lib/services/comicProcessor.ts
@@ -38,29 +38,31 @@ export function deriveFilenameFromUrl(url: string): string {
 let archiveManager: ArchiveManager | null = null;
 
 // Clean pages array to remove proxy objects and make it serializable
 function cleanPages(pages: any[]) {
   return pages.map((p) => ({
     filename: p.filename,
     index: p.index,
   }));
 }
 
-async function createThumbnail(blob: Blob): Promise<string> {
+export async function createThumbnail(
+  blob: Blob,
+  maxWidth = 200,
+  maxHeight = 300,
+): Promise<string> {
   return new Promise((resolve, reject) => {
     const img = new Image();
     const canvas = document.createElement("canvas");
     const ctx = canvas.getContext("2d");
 
     img.onload = () => {
-      const maxWidth = 200;
-      const maxHeight = 300;
       let width = img.width;
       let height = img.height;
 
       if (width > height) {
         if (width > maxWidth) {
           height *= maxWidth / width;
           width = maxWidth;
         }
       } else {
         if (height > maxHeight) {
diff --git a/src/routes/reader/+page.svelte b/src/routes/reader/+page.svelte
index e6c14b4..4a8ee57 100644
--- a/src/routes/reader/+page.svelte
+++ b/src/routes/reader/+page.svelte
@@ -3,20 +3,21 @@
 	import { onMount, onDestroy } from 'svelte';
 	import { goto } from '$app/navigation';
 	import { resolve } from '$app/paths';
 	import { get } from 'svelte/store';
 	import { currentComic, currentPageIndex, isLoading, error, currentFile, setError } from '$lib/store/session.js';
 	import ArchiveManager from '$lib/archive/archiveManager.js';
 	import ReaderShell from '$lib/ui/ReaderShell.svelte';
 	import type { ComicBook } from '../../types/comic.js';
 	import { comicStorage } from '$lib/storage/comicStorage.js';
 	import { logger } from '$lib/services/logger';
+	import { createThumbnail } from '$lib/services/comicProcessor';
 
 	let archiveManager: ArchiveManager;
 	let comic: ComicBook | null = null;
 	let file: File | null = null;
 	let archiveReady = false;
 
 	const unsubscribeComic = currentComic.subscribe((value) => {
 		comic = value;
 	});
 
@@ -78,20 +79,26 @@
 			archiveManager.cleanup();
 		}
 	});
 
 async function saveProgress(): Promise<void> {
 	if (!comic) return;
 	const pageIndex = get(currentPageIndex);
 	comic.currentPage = pageIndex;
 	comic.lastRead = new Date();
 	const totalPages = comic.totalPages || comic.pages?.length || 0;
+	try {
+		const pageBlob = await onExtractPage(pageIndex);
+		if (pageBlob) comic.lastReadThumbnail = await createThumbnail(pageBlob, 640, 960);
+	} catch (e) {
+		logger.warn('Reader', 'Failed to capture last-read page thumbnail', e);
+	}
 	try {
 		await comicStorage.saveComicMetadata(structuredCloneComic(comic));
 		await comicStorage.updateProgress(comic.id, pageIndex, totalPages);
 	} catch (error) {
 		logger.error('Reader', 'Failed to persist reading progress', error);
 	}
 }
 
 function structuredCloneComic(comic: ComicBook): ComicBook {
 	return {
diff --git a/src/types/comic.ts b/src/types/comic.ts
index c00b96e..0a47ef5 100644
--- a/src/types/comic.ts
+++ b/src/types/comic.ts
@@ -11,20 +11,21 @@ export interface ComicPage {
 
 export interface ComicBook {
   id: string;
   title: string;
   filename: string;
   pages: ComicPage[];
   currentPage: number;
   totalPages: number;
   lastRead: Date;
   coverThumbnail?: string; // Base64 encoded thumbnail
+  lastReadThumbnail?: string; // data URL of the last-read page (Continue-band background)
 
   // Custom filter configuration (numeric parameters only)
   customFilter?: FilterConfig;
 }
 
 export interface ArchiveEntry {
   filename: string;
   index: number;
   size: number;
   compressed_size?: number;
